# Dependencies for the Vercel Python functions (api/*.py).
# Pinned to known-good major ranges so a redeploy can't silently pull a breaking
# major (e.g. stripe 8 -> 15 changed StripeObject and broke .get()).
# NOT used by the scripts/cardify.sh CLI — that keeps using system Pillow + exiftool.
Pillow>=10,<13
piexif>=1.1.3,<1.2
Flask>=3.0,<4
PyJWT>=2.8,<3
cryptography>=42,<46
requests>=2.31,<3
stripe>=15,<16
